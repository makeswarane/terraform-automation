name: Terraform Infra Provision

on:
  push:
    paths:
      - 'modules/**'
      - 'main.tf'
      - 'variables.tf'
      - 'outputs.tf'
      - 'backend.tf'
      - 'terraform.tfvars'
    branches:
      - main
  pull_request:
    paths:
      - 'modules/**'
      - 'main.tf'
      - 'variables.tf'
      - 'outputs.tf'
      - 'backend.tf'
      - 'terraform.tfvars'

env:
  AWS_REGION: ap-south-1
  TF_STATE_BUCKET: ${{ secrets.TF_BUCKET }}
  TF_LOCK_TABLE: ${{ secrets.TF_LOCK_TABLE }}

jobs:
  terraform:
    name: Terraform Apply
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.6.7

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Terraform Init
        run: terraform init \
              -backend-config="bucket=${{ env.TF_STATE_BUCKET }}" \
              -backend-config="key=terraform/state.tfstate" \
              -backend-config="region=${{ env.AWS_REGION }}" \
              -backend-config="dynamodb_table=${{ env.TF_LOCK_TABLE }}" \
              -backend-config="encrypt=true"

      - name: Terraform Format Check
        run: terraform fmt -check

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        id: plan
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main'
        run: terraform apply -auto-approve tfplan

      - name: Save Terraform Output
        run: |
          terraform output -json > tf_output.json
        continue-on-error: false

      - name: Upload Terraform Output Artifact
        uses: actions/upload-artifact@v3
        with:
          name: tf-output
          path: tf_output.json

  notify:
    name: Slack / Email Notification
    runs-on: ubuntu-latest
    needs: terraform
    steps:
      - name: Download Terraform Output Artifact
        uses: actions/download-artifact@v3
        with:
          name: tf-output
          path: .

      # - name: Slack Notification
      #   uses: slackapi/slack-github-action@v1.23.0
      #   with:
      #     channel-id: ${{ secrets.SLACK_CHANNEL }}
      #     slack-message: "Terraform Infra Provision workflow completed with status: ${{ job.status }}"
      #     slack-token: ${{ secrets.SLACK_TOKEN }}

      - name: Send Email Notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "Terraform Infra Provision ${{ job.status }} for ${{ github.repository }}"
          body: |
            Hello Team,

            The Terraform Infra Provision workflow for repository `${{ github.repository }}` has completed.

            Status: ${{ job.status }}

            Terraform outputs saved in tf_output.json.
          to: ${{ secrets.NOTIFY_EMAIL }}
          from: ${{ secrets.EMAIL_USERNAME }}
